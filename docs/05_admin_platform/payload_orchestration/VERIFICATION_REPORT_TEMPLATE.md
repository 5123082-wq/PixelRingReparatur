# Verification Report (отчёт независимой проверки)

Проверяющий не является автором изменений и не исправляет найденные проблемы. Отсутствие проверки нельзя выдавать за успешную проверку.

## Identity (идентификация)

- Task ID (идентификатор задания):
- Verifier (проверяющий):
- Verification type (тип проверки):
- Verdict (вердикт): `PASS` (пройдено) / `PASS WITH RISKS` (пройдено с рисками) / `FAIL` (не пройдено)

## Scope Verified (проверенные границы)

## Source Evidence (исходные доказательства)

Указать реальные файлы, строки, артефакты и состояние окружения.

## Diff Reviewed (проверенная разница изменений)

Указать точные файлы и отделить ранее существовавшие изменения владельца от изменений Task ID (идентификатора задания).

## Commands Run (выполненные команды)

Для каждой команды указать рабочую папку, код завершения и краткий результат. Не включать значения секретов.

## Test Results (результаты тестов)

## Browser / API / Database Evidence (доказательства браузера, API и базы данных)

Для неприменимых областей написать `Not applicable` (не применяется). Для непроверенных областей написать `Not verified` (не проверено) и объяснить почему.

## Acceptance Criteria Results (результаты критериев приёмки)

Сопоставить каждый критерий с `PASS` (пройдено), `FAIL` (не пройдено) или `NOT VERIFIED` (не проверено) и доказательством.

## Security Findings (выводы по безопасности)

## Regression Risks (риски регрессий)

## Findings Requiring Changes (проблемы, требующие изменений)

Проверяющий не исправляет их в этом задании. Для исправления нужен новый Task ID (идентификатор задания).

## Unverified Areas (непроверенные области)

Перечислить все области, которые не были реально проверены.

## Recommended Status Transition (рекомендуемый переход статуса)

Указать допустимый переход из протокола статусов. `FAIL` (не пройдено) требует `CHANGES_REQUESTED` (требуются исправления) и отдельного задания.

## Proposed Progress Entry (предлагаемая запись прогресса)

- Date (дата):
- Current sprint/block (текущий спринт/блок):
- Verdict (вердикт):
- Evidence (доказательства):
- Unverified areas (непроверенные области):
- Findings/risks (выводы/риски):
- Recommended next action (рекомендуемое следующее действие):
