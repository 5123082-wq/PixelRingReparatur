---
description: Start or restart the local development server
---

// turbo-all
1. Start the Next.js development server in the `signage-service` directory.
```bash
cd signage-service && npm run dev
```

2. Wait for the server to be ready and verify it at `http://localhost:3000`.
